server:
  port: 8082
  
spring:
  application:
    name: SERVERPROVIDER
    
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/